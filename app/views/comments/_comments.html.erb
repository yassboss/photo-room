<%= form_with model: [commentable, Comment.new], id: "comment-form" do |f| %>
  <%= f.text_area :text, placeholder: 'コメントを記入', style: 'border: 1px solid gray;' %>
  <div class="actions">
    <%= f.submit "送信" %>
  </div>
<% end %>